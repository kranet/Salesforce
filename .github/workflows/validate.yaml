name: Validate pull request

on:
  pull_request:
    branches:
      - master
    types: [opened, synchronize]
    paths:
      - 'force-app/**'

jobs:
  validate-deployment:
    runs-on: ubuntu-latest
    steps:      
      - name: 'korbara'
        run: echo "lol"
